#include <stdio.h> //Informando ao programa que iremos aprroveitar dessa biblioteca (na verdade é um programa pré-programado)
#include <stdlib.h> // Outra biblioteca, faz com que o programa pause no final, não finalizando, aguardando o usuário.

int main() { //Corpo principal, função principal
    printf("\nBahia eh o mundo!\n\n");

    system("pause");
}